server:
  port: 8080
  mode: debug

postgres:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  database: smart_community
  sslmode: disable

jwt:
  secret: smart-community-secret
  expire: 604800